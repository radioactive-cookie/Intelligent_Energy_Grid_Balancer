# Backend API base URL.
# Defaults to the Render deployment. Override for local dev:
# VITE_API_URL=http://localhost:3001/api
VITE_API_URL=

# WebSocket URL for live grid updates.
# Defaults to the Render deployment. Override for local dev:
# VITE_WS_URL=ws://localhost:3001/ws
VITE_WS_URL=
