# Backend API base URL (e.g. https://your-backend.example.com/api)
# Leave empty to use the dev proxy (/api)
VITE_API_URL=

# WebSocket URL for live grid updates (e.g. wss://your-backend.example.com/ws)
# Leave empty to auto-detect from window.location
VITE_WS_URL=
